# Lori Player 项目约定

## 完成更新后必须重新安装

用户要求：完成 Lori 的功能开发或修复后，**重新构建正式版并安装到系统现有位置**，不要只改源码、运行测试或启动开发服务器。此要求是默认授权，除非用户明确要求本次不要安装，无需再次询问。

仅阅读、讨论或文档修改不需要重装。修改代码时保留用户已有改动。

当前系统安装采用当前用户级安装：

- 可执行文件：`/home/aliyah/.local/bin/lori-player`
- 应用菜单入口：`/home/aliyah/.local/share/applications/do.lori.player.desktop`
- 图标：`/home/aliyah/.local/share/icons/hicolor/256x256/apps/do.lori.player.png`
- Toolbox 条目：`lori-player`，启动命令为 `/home/aliyah/.local/bin/lori-player`

安装流程：

1. 运行与本次修改相关的测试和检查。
2. 在项目根目录运行 `npm run tauri -- build --no-bundle`。该命令会先构建前端，再生成嵌入最新前端资源的 Rust release 可执行文件；不要只运行 `cargo build` 后安装旧前端。
3. 将 `src-tauri/target/release/lori-player` 安装为上述系统可执行文件。先在目标目录创建临时文件、以 `755` 权限安装，再通过 `mv -f` 原子替换，避免直接覆盖正在运行的可执行文件。
4. 保持应用菜单入口、图标和 Toolbox 指向同一安装位置；需要时刷新桌面入口缓存。安装位置或启动命令改变时，同步更新 Toolbox（`toolbox add ... --force`）。
5. 比较构建产物与已安装文件的 SHA-256，验证桌面入口。若旧版正在运行，通过托盘菜单的「退出播放器」退出，再从系统安装路径重新启动。主窗口的 × 和窗口管理器关闭请求只会隐藏窗口，不能用来退出进程。不要用强制杀进程代替正常关闭，不要启动重复实例。
6. 确认新进程和主窗口正常，报告安装位置和验证结果。

重装不得删除或重置曲库、收藏、设置、听歌账本或其他应用数据。沿用应用标识 `do.lori.player`，避免将用户数据分散到新的应用目录。

## 听歌统计的数据约定

- 歌曲身份使用原始音频文件完整内容的 MD5；文件名、标题、艺术家只作标记。
- 时长从时间段账本演算，不以独立累计计数器替代账本。
- 保持导入的幂等合并、检查点单调增长、重叠区间去重与冲突事务回滚。
- 注意索引、分页、内存占用与实时更新成本；保存失败必须可见，不能静默丢弃待保存记录。
- 账本格式和合并细节见 `docs/listening-ledger.md`。

## 系统托盘与窗口生命周期

- 主窗口 × 和 Alt+F4 只隐藏窗口，保持播放、播放位置和桌面歌词继续运行。
- 系统托盘提供「显示播放器」和「退出播放器」；只有明确退出才终止应用及歌词窗口。
- 重复启动应恢复现有窗口，不创建重复播放器或重复托盘图标。
- 检查此功能时验证真实托盘注册、隐藏后进程存活、从托盘恢复，以及明确退出后的清理。
